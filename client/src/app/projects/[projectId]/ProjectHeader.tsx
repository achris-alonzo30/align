
interface ProjectHeaderProps {
    activeTab: string;
    setActiveTab: (activeTab: string) => void;
}
export const ProjectHeader = ({
    activeTab,
    setActiveTab
}: ProjectHeaderProps) => {
    return (
        <header>

        </header>
    )
}